package chefsolo
/*
import (
	"io/ioutil"

	"github.com/megamsys/gulp/meta"
	"gopkg.in/check.v1"
)
func (s *S) TestDownload(c *check.C) {
	c.Assert(s.config, check.NotNil)
	c.Assert(s.M, check.NotNil)
	c.Assert(meta.MC.Dir, check.NotNil)
	d := ioutil.Discard
	cr := NewChefRepo(s.M, d)
	c.Assert(cr, check.NotNil)
	err := cr.Download(true)
	c.Assert(err, check.IsNil)
}

func (s *S) TestTorr (c *check.C) {
	c.Assert(s.config, check.NotNil)
	c.Assert(s.M, check.NotNil)
	cr := NewChefRepo(s.M, ioutil.Discard)
	c.Assert(cr, check.NotNil)
	err := cr.Download(true)
	c.Assert(err, check.IsNil)
	err = cr.Torr()
	c.Assert(err, check.IsNil)
}
*/
